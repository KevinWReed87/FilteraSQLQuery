<h1>Filter a SQL query</h1>

<h2>List all organization machines</h2>

- Run a SQL query to retrieve only the device_id and operating_system columns from the machines table.

<img src="https://i.imgur.com/rQLDtqc.png" height="80%" width="80%" alt="Disk Sanitization Steps"/></p>

<h2>Retrieve a list of the machines with OS 2</h2>

- Select all the records from the machines table with a value of 'OS 2' in the operating_system column


<img src="https://i.imgur.com/PJ4BdaD.png" height="80%" width="80%" alt="Disk Sanitization Steps"/></p>

<h2>List employees in specific departments</h2>

- Filter the rows returned from department column in the employees table to include only employees from the 'Finance' department.

<img src="https://i.imgur.com/JQ6pQPZ.png" height="80%" width="80%" alt="Disk Sanitization Steps"/></p>

- Modify the previous query so that it returns employees who are in the 'Sales' department.

<img src="https://i.imgur.com/TzQi1U6.png" height="80%" width="80%" alt="Disk Sanitization Steps"/></p>

<h2>Identify employee machines</h2>

- Write a query to identify which employee uses the office in 'South-109'

<img src="https://i.imgur.com/VJBh4GP.png" height="80%" width="80%" alt="Disk Sanitization Steps"/></p>

- Modify the query you used in the previous step so that it returns information on all the employees in the 'South' building. Use the LIKE operator with % in this query.

<img src="https://i.imgur.com/8dsurwC.png" height="80%" width="80%" alt="Disk Sanitization Steps"/></p>

